import React, { useRef } from "react";
import ProfileContainer from "../layout/profile/ProfileContainer";

const Profilepage = () => {
  return (
    <div id="profile" className="w-full bg-primary-600">
      <ProfileContainer />
    </div>
  );
};

export default Profilepage;
